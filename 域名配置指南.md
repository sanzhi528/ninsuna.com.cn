# 奔腾科技网站域名配置指南

## 🌐 当前状态

**网站已开发完成，可通过以下方式访问：**
- ✅ **本地访问**：http://localhost:8081/index.html
- 🔄 **域名访问**：需要配置hosts文件（见下文）

## 📋 域名列表

| 域名 | 状态 | 用途 |
|------|------|------|
| www.bentiun.com | 待配置 | 主域名 |
| www.bentiun.com.cn | 待配置 | 备用域名 |
| www.bentiun.cn | 待配置 | 备用域名 |

## 🔧 本地测试配置方法

### 方法一：使用提供的脚本（推荐）

1. **运行域名配置脚本**：
   ```
   双击运行 setup_local_domains.bat
   ```
   - 此脚本会自动配置hosts文件
   - 需要管理员权限

2. **测试域名访问**：
   ```
   双击运行 test_domains.bat
   ```
   - 自动检测服务器状态
   - 测试域名解析
   - 一键打开测试页面

### 方法二：手动配置hosts文件

1. **打开hosts文件**：
   ```
   文件路径：C:\Windows\System32\drivers\etc\hosts
   ```

2. **添加以下内容**：
   ```
   127.0.0.1 www.bentiun.com
   127.0.0.1 www.bentiun.com.cn
   127.0.0.1 www.bentiun.cn
   # 奔腾科技本地测试域名配置
   ```

3. **保存文件**（需要管理员权限）

## 🌍 正式部署域名配置

### DNS解析配置（生产环境）

在域名注册商处配置DNS解析记录：

```
类型   名称               值                TTL
A      www.bentiun.com    您的服务器IP      3600
A      www.bentiun.com.cn 您的服务器IP      3600  
A      www.bentiun.cn     您的服务器IP      3600
```

### Web服务器配置

#### Nginx配置示例：
```nginx
server {
    listen 80;
    server_name www.bentiun.com www.bentiun.com.cn www.bentiun.cn;
    
    root /var/www/bentiun_tech;
    index index.html;
    
    location / {
        try_files $uri $uri/ =404;
    }
}
```

#### Apache配置示例：
```apache
<VirtualHost *:80>
    ServerName www.bentiun.com
    ServerAlias www.bentiun.com.cn www.bentiun.cn
    DocumentRoot /var/www/bentiun_tech
    
    <Directory /var/www/bentiun_tech>
        Options Indexes FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>
```

## 🚀 测试步骤

### 本地测试
1. 确保服务器运行：`python -m http.server 8081`
2. 配置hosts文件
3. 测试访问：
   - http://www.bentiun.com:8081/index.html
   - http://www.bentiun.com.cn:8081/index.html
   - http://www.bentiun.cn:8081/index.html

### 生产环境测试
1. 上传网站文件到服务器
2. 配置DNS解析
3. 配置Web服务器
4. 测试访问（无需端口号）：
   - http://www.bentiun.com
   - http://www.bentiun.com.cn
   - http://www.bentiun.cn

## ⚠️ 注意事项

1. **hosts文件修改需要管理员权限**
2. **DNS解析生效需要时间（通常24-48小时）**
3. **生产环境建议使用HTTPS**
4. **建议配置301重定向，将非www域名跳转到www域名**

## 📞 技术支持

如果遇到配置问题，可以：

1. 检查服务器是否正常运行
2. 确认hosts文件配置正确
3. 清除浏览器缓存
4. 使用`ping`命令测试域名解析

## ✅ 完成状态

- [x] 网站开发完成
- [x] 本地服务器运行正常
- [ ] 本地域名配置
- [ ] 生产环境部署
- [ ] 正式域名解析

**当前建议：先使用本地访问测试所有功能，确认无误后再进行正式域名配置。**